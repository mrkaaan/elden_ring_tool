我目前需要开发一个插件，用于给艾尔登法环这个游戏便捷的导入/覆盖存档以及打入mod
上面两个功能是刚需，是必须要开发的，下面是考虑增加的内容是 
打开指定的文件夹
修改指定的mod文件
导出存档

使用背景是这样的，我自己没有能打游戏的电脑，玩游戏都是和朋友一起去网吧玩老头环，每次去都需要打开指定的不同文件导入存档，打入联机mod（有时候可能要改一下mod的配置文件）。然后再使用mod文件启动游戏。游玩结束后还需要导出存档

所以我要开发一个工具
你来帮我选定一下技术栈看看什么合适，要开发便捷，开发环境搭建简单，因为我还有一个小时就要去上网要用到
是用原生的cmd写的bat脚本，还是使用python呢，这两个选一个。如果是python你不用教我怎么搭建环境，说清楚依赖库就行

下面是具体的开发细节

一 先说打mod
无缝连接mod主要是这两个文件
SeamlessCoop
ersc_launcher.exe

要放到 Steam库 - 游戏列表中 - 右键艾尔登法环 - 管理 - 浏览本地文件 - 再打开Game目录
这个位置中
第一个难点是如何去在不同的电脑中去定位这个文件夹
总不能去模拟点击，开发起来太臃肿了而且不可能适配不同电脑

但是实际使用过程中，可能联机mod的文件是以下几种形式存在
1 无缝联机mod.rar  一个压缩包可能任意格式
2 无缝联机mod  一个文件夹 文件夹里面有可能嵌套一个文件夹，再打开才是两个联机文件，也可能打开就是两个联机文件
3 SeamlessCoop  ersc_launcher.exe  这一个文件夹和一个文件直接存在没有嵌套文件夹
我的理想情况下是任意模式下都能直接使用这个工具导入mod
这是这个功能第二个难点

二 导入存档
导入存档主要是这一个文件夹
EldenRing

这个文件夹可能是一个压缩包
EldenRing.rar 这个解压有可能嵌套也可能不嵌套 不嵌套的情况下里面就是存档文件
25-12-1存档备份.rar  这个解压之后里面就有一个EldenRing
也可能是直接一个EldenRing文件夹

把这一个文件夹EldenRing（不嵌套的）覆盖
C:\Users\Administrator\AppData\Roaming\EldenRing
（其中的Administrator是当前系统用户名称，默认是Administrator）

也可以使用此命令打开上面的文件夹：
使用 Win + R 打开 “运行” 窗口
输入：%appdata%/EldenRing

（这里如果是覆盖的话其实打开文件夹到...Roaming\就行了，命令行也只需要%appdata%就行了）

三 要考虑增加的功能

1 导出存档这个也好说就是把系统中的EldenRing直接复制出来，然后压缩成一个文件就行了，但是最好是这样处理一下
先建一个文件夹 日期要有时分秒_存档备份，然后再把压缩好的EldenRing放进去，这样比较好管理

2 打开指定的文件夹 这个也好说，就是一个存档位置，一个mod放置的位置

3 修改指定的mod文件
就是修改老头环游戏目录下Game目录下的SeamlessCoop文件夹
的ersc_settings，这个是ini格式的文件，修改好直接保存而不是另存为
将death_debuffs = 1 改成 death_debuffs = 0
这个是联机密码 cooppassword = 4820
4820 可以替换为任意四位数字，只要保持联机双方该数字一致即可

这个功能的话就有个按钮“获取目前mod状态”类似的文字，先点一下获取目前这个文件的装填，按钮下边有小字可以显示有这几种状态
- 未导入mod文件，就是缺失mod文件，就没有办法配置
- 显示death_debuffs的值 右边显示联机的密码
然后这个“获取mod状态的”按钮周边有一个输入框，用来输入四位数的联机密码，然后还有一个按钮“修改联机密码”，点击确认就应用这个联机密码，下面的小字也会跟着变，还有一个按钮是设置death_debuffs的值为0，还有一个按钮是设置death_debuffs的值为1，这两个设death_debuffs的值按钮都是点击立即应用


有几个要考虑的，
1是写出的工具，以及需要用到的文件是位置之间是怎么样的
比如这个工具和需要使用文件处于同一个目录下

2是需要用到的文件的格式，前面我也提到了，可能是压缩包可能是文件夹也可能是直接散落的文件
我想的是可以去做一些适配，但是不能无止境的适配，还是需要有一定限制才行，在我说的几种文件的情况下，看下哪个可以适配哪个不用适配，比如说一个压缩文件 A 解压之后里面还有一个 A文件夹，是不是就不用适配了，必须要要求压缩文件A解压之后就直接是需要的文件

3这个是我灵光一闪想到的，可以在工具运行一开始就先去检索Steam游戏库的地址，这样一是方便能实现打开老头环游戏文件夹的功能，二是也方便实现导入存档的功能

4 这些目录什么可以的话最好是用一些全局的变量去存储，方便我调试

好像功能需求写着写着就不能用bat实现了，那就用python+tk吧